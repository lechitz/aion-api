# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/api
/seed-caller
/seed-helper

# Test binary, built with `go test -c`
*.test

# Output of the go coverage adapter, specifically when used with LiteIDE
*.out
coverage.html
report.json
junit-report.xml

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env.dev
.env.prod
.env.local
.env.my
**/seed/.env.local

# IDE files
.idea
.qodo

.venv-docs/
site/
/infrastructure/docker/environments/dev/.env

# Personal "my" environment - keep your local configs private
/infrastructure/docker/environments/my/

# Personal, local-only docs (private workspace for docs/ideas)
docs/personal/
notes/
**/docs.md
agents/
scripts/
!/.github/scripts/
!/.github/scripts/**

# Local module cache used by Go
.gocache
.gomodcache/

# Go build cache
.gotmp/

# Local caches
/.cache/
backups/

# Air hot-reload temporary directory
tmp/
build-errors.log
.cache/

# Local seed env and generated files
/infrastructure/db/seed/.env
/infrastructure/db/seed/api_success.log
/bin/seed-helper
*.env.backup*
tmp/

# Test data work tags
infrastructure/db/seed/test_timeline_data.sql
